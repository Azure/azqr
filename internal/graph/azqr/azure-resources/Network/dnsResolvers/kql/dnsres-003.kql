// Azure DNS Private Resolver SLA
resources
| where type =~ 'Microsoft.Network/dnsResolvers'
| extend recommendationId = 'dnsres-003'
| extend param1 = '99.99%'
| project recommendationId, name, id, tags, param1
