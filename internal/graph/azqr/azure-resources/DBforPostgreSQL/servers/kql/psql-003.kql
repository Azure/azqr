// Azure Resource Graph Query
// Find PostgreSQL servers (Single Server) and report their SLA
// Note: Single Server has fixed SLA of 99.99%
resources
| where type =~ 'Microsoft.DBforPostgreSQL/servers'
| project recommendationId = 'psql-003', name, id, tags, param1 = "99.99%"
