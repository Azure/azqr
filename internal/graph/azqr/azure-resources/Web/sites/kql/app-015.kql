// Azure Resource Graph Query
// Find App Services that have Client Affinity enabled
resources
| where type =~ "Microsoft.Web/sites"
| where kind !contains "function" and kind !contains "workflowapp"
| where properties.clientAffinityEnabled == true
| project recommendationId = "app-015", name, id, tags, param1 = "Client Affinity is enabled"
